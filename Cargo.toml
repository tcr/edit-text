[workspace]
members = [
    "build-tools",
    "oatie",
    "edit-common",
    "edit-server",
    "edit-client",
]

[profile.dev]
debug = true

[profile.release]
debug = true

[patch.crates-io]
wasm-bindgen = { path = "../wasm-bindgen" }
wasm-bindgen-cli-support = { path = "../wasm-bindgen/crates/cli-support" }
